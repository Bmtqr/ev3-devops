apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mysql-pvc
  namespace: tienda
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: gp2 # La clase de almacenamiento estándar de AWS
  resources:
    requests:
      storage: 10Gi # Reserva 10 Gigabytes en AWS EBS
